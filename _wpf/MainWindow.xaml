<Window x:Class="PraxisWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:taskViewer="clr-namespace:PraxisWpf.Features.TaskViewer"
        xmlns:controls="clr-namespace:PraxisWpf.Controls"
        Title="Praxis WPF Task Manager" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Name="MainAppWindow">
    
    <!-- No Window.InputBindings - all key handling at data level -->
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CyberControlBrush}" 
                BorderBrush="{DynamicResource CyberBorderBrush}" 
                BorderThickness="0,0,0,2" Padding="10">
            <TextBlock Text="╔═══ PRAXIS TASK MANAGER ═══╗" 
                       Style="{DynamicResource CyberHeaderText}"/>
        </Border>

        <!-- Main content -->
        <taskViewer:TaskView Grid.Row="1" DataContext="{Binding TaskViewModel}"/>

        <!-- Enhanced Status bar with operational feedback -->
        <controls:StatusBar Grid.Row="2" DataContext="{Binding StatusService}"/>
        
        <!-- Keyboard shortcuts help - shown when no status messages -->
        <StatusBar Grid.Row="2" Visibility="{Binding StatusService.IsVisible, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <StatusBarItem>
                <TextBlock Text="N: New | E: Edit | Del: Delete | +/-: Expand/Collapse | Ctrl+S: Save | Ctrl+J/K: Navigate | Alt+B: Back"
                          ToolTip="Keyboard shortcuts for efficient task management"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>